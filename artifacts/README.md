# Local artifacts

このフォルダの `.blend` はGit管理対象外です。商用商品データを含むため、公開リポジトリへ追加しません。

必要なローカルファイル:

- `Lasyusha-v002.blend` — 現在の確定版（7個の管理対象メッシュ）。
- `Lasyusha-baseline.blend` — 加工前比較用。

`python tools/hairflow.py status` で存在とSHA-256を確認できます。

旧v001はv002の復元テスト後に削除済み。過去のスクリプトは履歴として残すが、旧版の `.blend` を維持する運用ではない。
